<h1>Listing main_menu_items</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Position</th>
    <th>Page</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @main_menu_items.each do |main_menu_item| %>
  <tr>
    <td><%= main_menu_item.title %></td>
    <td><%= main_menu_item.position %></td>
    <td><%= main_menu_item.page_id %></td>
    <td><%= link_to 'Show', main_menu_item %></td>
    <td><%= link_to 'Edit', edit_main_menu_item_path(main_menu_item) %></td>
    <td><%= link_to 'Destroy', main_menu_item, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Main menu item', new_main_menu_item_path %>
