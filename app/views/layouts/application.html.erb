<!DOCTYPE html>
<html>
<head>
  <link href='http://fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
  <meta name="viewport" content="width=device-width; initial-scale=1; maximum-scale=1">
  <title>Two Weeks from Everywhere Productions</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= jquery_include_tag :google, :force => true %>
  <%= javascript_include_tag "application" %>

  <%= yield :head %>

  <%= csrf_meta_tags %>

</head>
<body class="<%= user_signed_in? and current_user.view_as_admin? ? 'view-admin-true' : 'view-admin-false' %>">
  
  <div class="container-fluid">

    <%= render :partial => "elements/admin_menu" %>

    <% if flash[:notice] or alert %>
      <p class="alert" style="margin: 0px;">
        <%= flash[:notice] %>
        <%= alert %>
      </p>
    <% end %>

    <div id="main">

      <div class="row-fluid">

        <div class="span3">
          &nbsp;
        </div>

        <div class="span9">

          <div class="vizblock-dark">
            <div class="default-padding">
              <%= yield %>
            </div>
          </div>

        </div>
      </div>
    </div>
    <%= render :partial => "elements/footer" %>
  </div>

</body>
</html>
